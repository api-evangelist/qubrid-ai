naftiko: 1.0.0-alpha2
info:
  label: Qubrid AI RAG API — Knowledge Bases
  description: 'Qubrid AI RAG API — Knowledge Bases. 4 operations. Lead operation: List knowledge bases. Self-contained Naftiko
    capability covering one Qubrid Ai business surface.'
  tags:
  - Qubrid Ai
  - Knowledge Bases
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    QUBRID_AI_API_KEY: QUBRID_AI_API_KEY
capability:
  consumes:
  - type: http
    namespace: rag-knowledge-bases
    baseUri: https://platform.qubrid.com/api/v1
    description: Qubrid AI RAG API — Knowledge Bases business capability. Self-contained, no shared references.
    resources:
    - name: rag-knowledge-bases
      path: /rag/knowledge-bases
      operations:
      - name: listknowledgebases
        method: GET
        description: List knowledge bases
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: createknowledgebase
        method: POST
        description: Create a knowledge base
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    - name: rag-knowledge-bases-knowledge_base_id
      path: /rag/knowledge-bases/{knowledge_base_id}
      operations:
      - name: getknowledgebase
        method: GET
        description: Retrieve a knowledge base
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
      - name: deleteknowledgebase
        method: DELETE
        description: Delete a knowledge base
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.QUBRID_AI_API_KEY}}'
  exposes:
  - type: rest
    namespace: rag-knowledge-bases-rest
    port: 8080
    description: REST adapter for Qubrid AI RAG API — Knowledge Bases. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/rag/knowledge-bases
      name: rag-knowledge-bases
      description: REST surface for rag-knowledge-bases.
      operations:
      - method: GET
        name: listknowledgebases
        description: List knowledge bases
        call: rag-knowledge-bases.listknowledgebases
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createknowledgebase
        description: Create a knowledge base
        call: rag-knowledge-bases.createknowledgebase
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
    - path: /v1/rag/knowledge-bases/{knowledge-base-id}
      name: rag-knowledge-bases-knowledge-base-id
      description: REST surface for rag-knowledge-bases-knowledge_base_id.
      operations:
      - method: GET
        name: getknowledgebase
        description: Retrieve a knowledge base
        call: rag-knowledge-bases.getknowledgebase
        outputParameters:
        - type: object
          mapping: $.
      - method: DELETE
        name: deleteknowledgebase
        description: Delete a knowledge base
        call: rag-knowledge-bases.deleteknowledgebase
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rag-knowledge-bases-mcp
    port: 9090
    transport: http
    description: MCP adapter for Qubrid AI RAG API — Knowledge Bases. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: list-knowledge-bases
      description: List knowledge bases
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rag-knowledge-bases.listknowledgebases
      outputParameters:
      - type: object
        mapping: $.
    - name: create-knowledge-base
      description: Create a knowledge base
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: rag-knowledge-bases.createknowledgebase
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
    - name: retrieve-knowledge-base
      description: Retrieve a knowledge base
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rag-knowledge-bases.getknowledgebase
      outputParameters:
      - type: object
        mapping: $.
    - name: delete-knowledge-base
      description: Delete a knowledge base
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: rag-knowledge-bases.deleteknowledgebase
      outputParameters:
      - type: object
        mapping: $.
