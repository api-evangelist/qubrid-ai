naftiko: 1.0.0-alpha2
info:
  label: Qubrid AI Inference API — Embeddings
  description: 'Qubrid AI Inference API — Embeddings. 1 operations. Lead operation: Create embeddings. Self-contained Naftiko
    capability covering one Qubrid Ai business surface.'
  tags:
  - Qubrid Ai
  - Embeddings
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    QUBRID_AI_API_KEY: QUBRID_AI_API_KEY
capability:
  consumes:
  - type: http
    namespace: inference-embeddings
    baseUri: https://platform.qubrid.com/v1
    description: Qubrid AI Inference API — Embeddings business capability. Self-contained, no shared references.
    resources:
    - name: embeddings
      path: /embeddings
      operations:
      - name: createembedding
        method: POST
        description: Create embeddings
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.QUBRID_AI_API_KEY}}'
  exposes:
  - type: rest
    namespace: inference-embeddings-rest
    port: 8080
    description: REST adapter for Qubrid AI Inference API — Embeddings. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/embeddings
      name: embeddings
      description: REST surface for embeddings.
      operations:
      - method: POST
        name: createembedding
        description: Create embeddings
        call: inference-embeddings.createembedding
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: inference-embeddings-mcp
    port: 9090
    transport: http
    description: MCP adapter for Qubrid AI Inference API — Embeddings. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: create-embeddings
      description: Create embeddings
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: inference-embeddings.createembedding
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
